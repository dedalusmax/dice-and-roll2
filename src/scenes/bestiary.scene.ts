export class BestiaryScene extends Phaser.Scene {

    constructor() {
        super({
            key: "BestiaryScene"
        });
    }

    // no init, since no scene parameters passed
    
    create(): void {
    }
}
